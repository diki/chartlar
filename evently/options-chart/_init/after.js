function(){
  var widget = $(this);
  var app = $$(this).app;
  //$("#chart-color", widget).evently("colorPalette", app, ["chart", "backgroundColor", "Background Color"]);
  //$("#plot-color", widget).evently("colorPalette", app, ["chart", "plotBackgroundColor", "Background Color"]);
}
